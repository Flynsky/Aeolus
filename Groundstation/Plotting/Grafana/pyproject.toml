[tool.poetry]
name = "grafana-gs"
version = "0.1.0"
description = ""
authors = ["Fynn <f.gewiese@gmx.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pymongo = "^4.10.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
